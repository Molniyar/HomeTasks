#include <iostream>

/*
int main() {
	int count;std::cin >> count;
	int cost;std::cin >> cost;
	int price;std::cin >> price;

	int marks = price / cost + (price % cost ? 1 : 0);
	std::cout << count / marks;
}
//*/