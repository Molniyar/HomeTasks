#include <iostream>

/*
int main() {
	int length; std::cin >> length;
	int array[1000];

	int result = 1001;
	for (int i = 0; i < length; i++) {
		int inp; std::cin >> inp;
		if (inp > 0 && inp < result) result = inp;
		array[i] = inp;
	}
	std::cout << (result == 1001 ? 0 : result) << '\n';
	for (int i = 0; i < length; i++) {
		std::cout << array[i] << ' ';
	}
}
//*/